name: cert-manager

userID: 1000
groupID: 1000

global:
  image:
    registry: quay.io

image:
  name: giantswarm/cert-manager-controller
  # when updating the tag please make sure to keep appVersion in Chart.yaml in sync
  tag: v0.15.1

resources:
  requests:
    cpu: 50m
    memory: 100Mi
